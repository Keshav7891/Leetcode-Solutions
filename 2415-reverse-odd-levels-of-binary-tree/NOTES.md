if(curr->left->left != NULL)
{
cll = curr->left->left;
}
if(curr->left->left != NULL)
{
cll = curr->left->left;
}
if(curr->left->left != NULL)
{
cll = curr->left->left;
}
if(curr->left->left != NULL)
{
cll = curr->left->left;
}